# jsonの読み書き担当 モデル　→←　json
# service → repositroy → storage 
# 役割 : CRUD → save 開いた時に、　loadする
# 3
   # def get_by_user(self,user_id): #リストで返す
    #     return [ r for  r in self.routine.values() if r.user_id == user_id ]